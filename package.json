{
  "name": "nexus-labs-up",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio oficial de Nexus Labs — plataforma de clubes estudiantiles de la Universidad Panamericana.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "build:unsafe": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cf:preview": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "cf:types": "wrangler types"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource-variable/inter": "^5.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@cloudflare/workers-types": "^5.20260919.1",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.7.3",
    "wrangler": "^4.135.0"
  },
  "engines": {
    "node": ">=18.20.0"
  }
}
